require 'spec_helper'

describe Terraforming::Resource::DnsimpleRecord do
  it 'has a version number' do
    expect(Terraforming::Resource::DnsimpleRecord::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
